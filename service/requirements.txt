# Parsing and data types (milestone 1), retrieval (milestone 2), the model
# client (milestone 4). Serving is added with its milestone.
pydantic==2.12.5
pyyaml==6.0.3
bm25s==0.3.11
PyStemmer==3.1.0
fastembed==0.8.0
numpy==2.5.3
tiktoken==0.14.0
anthropic==1.4.0
# Serve layer: the page and the JSON routes.
fastapi==0.141.1
uvicorn==0.52.4
# TestClient transport for the web tests; also the HTTP client the SDK uses.
httpx==0.28.1
