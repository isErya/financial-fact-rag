# Copy to .env. Every value has a default; the fake backend needs none of
# them. The key is read only when LLM_MODEL_BACKEND=anthropic.
LLM_MODEL_BACKEND=fake
LLM_MODEL=claude-opus-5
ANTHROPIC_API_KEY=
WEB_PORT=8804
# 1 embeds every chunk during the index build (slow on a laptop CPU).
DENSE=0
